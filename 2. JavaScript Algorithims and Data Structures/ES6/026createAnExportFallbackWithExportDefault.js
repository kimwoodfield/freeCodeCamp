// The following function should be the fallback value for the module. Please add the necessary code to do so.

export default function subtract(x, y) { // exports as a default fallback function
    return x - y;
  }