// Modify the function increment by adding default parameters so that it will add 1 to number if value is not specified.

// Only change code below this line
// const increment = (number, value) => number + value;
const increment = (number, value = 1) => number + value;
// creates a variable called 'increment' that stores a function. the function takes two values that returns the first value plus the second value. In the case that no value is provided for the second value, consider it as equal to 1.

// Only change code above this line

