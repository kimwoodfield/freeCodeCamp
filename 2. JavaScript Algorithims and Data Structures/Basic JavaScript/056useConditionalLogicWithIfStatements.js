// Create an if statement inside the function to return "Yes, that was true" if the parameter wasThatTrue is true and return "No, that was false" otherwise.

function trueOrFalse(wasThatTrue) { // function created, takes a value

    // Only change code below this line
  
    if (wasThatTrue) { // If statement is created, IF a value is passed execute this
        return "Yes, that was true"; // if a value was passed, return this.
    }
    return "No, that was false"; // if no value was passed, return this.
  
    // Only change code above this line
  
  }
  