// Push ["dog", 3] onto the end of the myArray variable.

/* // Setup
var myArray = [["John", 23], ["cat", 2]];

// Only change code below this line
*/

var myArray = [ ["John", 23], ["cat", 2] ];

myArray.push(["dog", 3]);
// We can use the push() function to push an item or value onto the end of a pre-existing variable.
// In this case, we are saying we want the array ["dog", 3] to be pushed onto the end of the 'myArray' variable, which is an array in itself.

// Push() adds to the end.