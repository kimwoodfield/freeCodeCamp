// Change the 0.0 so that quotient will equal to 2.2.
// var quotient = 0.0 / 2.0;

var quotient = 4.4 / 2.0; // 4.4 divided by 2 equals 2.2, so the new value stored in the variable 'quotient' will be 2.2.