/* Make an object that represents a dog called myDog which contains the properties "name" (a string), "legs", "tails" and "friends".

You can set these object properties to whatever values you want, as long as "name" is a string, "legs" and "tails" are numbers, and "friends" is an array. */


var myDog = { // a variable is created called 'myDog' and stores an object.

    // Only change code below this line
    
    "name": "Tyson", // the first property in the object is a string "name" and this property stores another string with the value of "Tyson".
    "legs": 4, // the second property in the object is a string "legs" and this property stores a number of 4.
    "tails": 1, // the third property in the object is a string "tails" and this property stores a number of 1.
    "friends": ["rover", "billy"] // the fourth property in the object is a string "friends" and this property stores an array with two strings inside "rover" and "billy".
    
    // Only change code above this line

    };
    