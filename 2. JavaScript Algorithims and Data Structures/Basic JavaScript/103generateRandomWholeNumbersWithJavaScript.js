// Use this technique to generate and return a random whole number between 0 and 9.

function randomWholeNum() { // function created

    // Only change code below this line
  
    // return Math.random();
    return Math.floor(Math.random() * 10); // find a random number between 0 and 9 with Math.random multiplied by 10. Math floor then rounds the new number to the next whole number down.

  }