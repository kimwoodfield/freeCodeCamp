// Change randomFraction to return a random number instead of returning 0.

function randomFraction() { // function created called randomFraction

    // Only change code below this line
  
    // return 0;
    return Math.random(); // when function is called, return a random number between 0 and not quite up to 1..
  
    // Only change code above this line
  }
  