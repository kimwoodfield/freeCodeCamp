// Read the values of the properties "an entree" and "the drink" of testObj using bracket notation and assign them to entreeValue and drinkValue respectively.

// Setup
var testObj = { // variable created called 'testObj' that stores an object.

    "an entree": "hamburger", // first property of the object is a string "an entree" which holds a string of "hamburger"
    "my side": "veggies", // second property of the object is a string "my side" which holds a string of "veggies"
    "the drink": "water" // third property of the object is a string "the drink" which holds a string of "water"
  };
  
  // Only change code below this line
  
  var entreeValue = testObj["an entree"];   // Change this line // variable created with name 'entreeValue', the property "an entree" is taken from the 'testObj' variable /array and its contents is stored inside the variable 'entreeValue'.

  var drinkValue = testObj["the drink"];    // Change this line // variable created with name 'drinkValue', the property "the drink" is taken from the 'testObj' variable /array and its contents is stored inside the variable 'entreeValue'.
  