/* 
1. Create a function called reusableFunction which prints "Hi World" to the dev console.
2. Call the function.
*/ 

// reusableFuntion = () => console.log("Hi World");


function reusableFunction() { // We create a function called 'reusable function'. 
    console.log("Hi World"); // When the function is called, "Hi World" is printed into the browser console.
}

reusableFunction(); // We call the function so that it actually executes. Remember that the function does not start working until we call it.