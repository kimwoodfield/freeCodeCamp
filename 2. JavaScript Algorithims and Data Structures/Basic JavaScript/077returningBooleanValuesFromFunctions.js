// Fix the function isLess to remove the if/else statements.

// function isLess(a, b) {
//     // Only change code below this line
//     if (a < b) {
//       return true;
//     } else {
//       return false;
//     }
//     // Only change code above this line
//   }
  
//   isLess(10, 15);


function isLess(a, b) { // function is created, takes two values

    // Only change code below this line

    return a < b; // comparison operators return a boolean (true or false).
    // If the first value passed is less than the second value passed, return whether this is true or false.

    // Only change code above this line

  }
  
  isLess(10, 15); // this will return true, as 10 is less than 15.