// Use parseInt() in the convertToInteger function so it converts the input string str into an integer, and returns it.

function convertToInteger(str) { // function created, takes a string

    return parseInt(str) // return the string passed in as an integer

}

convertToInteger("56"); // will return 56 as an integer instead of a string.
