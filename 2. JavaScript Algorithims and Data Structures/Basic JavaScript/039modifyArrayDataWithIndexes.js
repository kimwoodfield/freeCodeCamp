// Modify the data stored at index 0 of myArray to a value of 45.

/* // Setup
var myArray = [18,64,99];

// Only change code below this line
*/

var myArray = [18, 64, 99];

myArray[0] = 45;
// Earlier it was said that letters in strings could not be changed using indexing, well the same does not apply for numbers in an array. We can use bracket notation to mutate an array to include a number that replaces a prexisting number.